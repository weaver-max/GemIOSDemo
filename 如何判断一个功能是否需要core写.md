# 如何判断一个功能该谁写

开发新功能前先过一遍这张卡片，决定是自己写还是找 core。

---

## 一句话标准

> **非 KV 存储的、纯计算的、平台无关的、且双端必须一致的 → core 写。
> 其余全在 App。**

关系型数据的存储、更新、查询，**core 一概不参与**。

---

## 三步判断

```
第 1 步：涉及关系型数据的存/改/查吗？
        └─ 是 ──► App 自己闭环，到此结束，别再往下想
        └─ 否 ──► 继续

第 2 步：是纯计算、平台无关吗？
        （不碰系统 API、不碰 UI、不碰本地库）
        └─ 否 ──► App 写
        └─ 是 ──► 继续

第 3 步：两端实现得不一样，是 bug 吗？
        └─ 是 ──► 🔴 找 core 写
        └─ 否 ──► App 写
```

**三步全过才找 core。** 只要第 1 步命中，后面不用想了。

---

## 已经定好的几类，不用再判断

| 功能 | 谁写 | 说明 |
|---|---|---|
| **交易历史 / 代币发现** | **App** | 直连后端数据服务。链上 RPC 查不了这两样 |
| 其余链上查询 | **core** | 走节点 RPC，App 提供节点地址 |
| 本地关系型存储 | **App** | 完全闭环 |
| 网络调用的**执行** | **App** | core 的对外请求全走 App 的接口 |
| KV 存储的**执行** | **App** | core 唯一直接接触的存储类型 |
| 私钥 / 助记词 json | **core** | 唯一由 core 独占的文件 |

---

## 举几个例子

### ✅ 该找 core 写

| 功能 | 为什么 |
|---|---|
| 手续费估算（含 EIP-1559） | 纯计算，两端算出不同数字 = bug |
| 地址格式校验 / EIP-55 | 纯计算，一端判合法一端不合法 = bug |
| 交易构造与签名 | 纯计算 + 密码学，错了丢钱 |
| 金额精度换算 | 纯计算，差一位就是事故 |
| 链的默认参数（滑点、派生路径） | 双端必须同一份 |

### ❌ 自己写

| 功能 | 为什么 |
|---|---|
| 资产列表排序、分页、搜索 | 要 join 本地多张表 → 第 1 步就出局 |
| 地址簿、自定义节点、偏好设置 | 关系型存储 + 用户输入 |
| 交易历史列表与筛选 | 数据来自后端，存查都在本地 |
| 下拉刷新、动效、骨架屏 | 平台相关，两端本就该不同 |
| 日期 / 货币格式化 | 跟随系统 locale，不需要一致 |
| 推送、生物识别、剪贴板 | 只有平台 API 能做 |

---

## 容易判错的两种

**① 把"要一致"当成"必须 core 写"**

证书固定、安全存储、生物识别 —— 这些确实要求双端行为一致，
但**只有平台 API 能做**，第 2 步就出局了。
这类的一致性靠**同一份验收清单**保证，不靠架构。

**② 把"简单"当成"自己写就行"**

"这个规则很简单，两边各写一下得了" —— 然后上线发现两端不一致，
再回头统一就是返工。

判断依据不是复杂度，是**不一致算不算 bug**。

---

## 提给 core 之前，先自问

```
□ 确认过它不需要读写关系型数据吗？
□ 确认过它不依赖任何平台 API 吗？
□ 两端各写一遍真的会出问题吗？（不是"可能不一致"，是"不一致就是 bug"）
□ 这个规则稳定吗？还是会跟着运营频繁调整？
```

最后一条容易忽略：**改 core 要发新版、双端同步升级**。
会频繁变的规则（首页推荐位、A/B 实验、活动配置）该走后端配置，不是 core。

---

## 分工的两条底层原因

不是拍脑袋定的，是被这两条约束逼出来的：

**① 私钥不能过 FFI** —— 所以密码学必须在 core 内部闭环，
keystore 文件也只能由 core 读写。

**② 响应式查询只有平台给得了** —— GRDB 的 `ValueObservation` /
Room 的 `Flow`，数据一变 UI 自动刷新。
数据库放 core 里就拿不到这个能力，整个数据流都得退回手动轮询。

想清楚这两条，大部分边界问题自己就有答案了。

---

延伸阅读：

- [App 端和 Core 端的功能边界](app端和core端的功能边界.md) —— 接口方向、网络与存储的完整划分
- [App 的数据从哪来](app数据来源.md) —— 四个数据来源与后端端点清单
